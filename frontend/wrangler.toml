name = "utonga-cons"
compatibility_date = "2024-03-05"
pages_build_output_dir = "dist"

[vars]
# The base URL for your deployed Django backend
# Replace this with your actual backend production URL
VITE_API_BASE_URL = "http://localhost:8000"

[build]
command = "npm run build"
watch_dir = "src"

[site]
bucket = "./dist"
